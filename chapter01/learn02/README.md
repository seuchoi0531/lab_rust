## 01-02
변수는 `let`으로 선언한다.
선언한 변수는 99% 확률로  유추
만약에 유추가 안되면 타입 지정 가능
타입 지정은 `let x: f64;`처럼 지정

변수에 값을 재할당 할 수 있음
```
let x = 13;
println!("{}", x); // 13

let x = 3.3;
println!("{}", x); // 3.3
```
이 때 타입이 달라도 괜찮음

변수명은 언제나 `snake_case`여야 함

https://tourofrust.com/03_ko.html